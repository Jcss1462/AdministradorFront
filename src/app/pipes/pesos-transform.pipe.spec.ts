import { PesosTransformPipe } from './pesos-transform.pipe';

describe('PesosTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new PesosTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
